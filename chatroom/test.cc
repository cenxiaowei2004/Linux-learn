

#include "client.hpp"

int main(int argc, char* argv[]) {
    // do this later:
    // if (argc < 2) {
    //     printf("format: ./Main ip port") return 1;
    // }
    client c;
    c.Run();
    return 0;
}